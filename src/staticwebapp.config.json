{
    "routes": [
        {
            "route":"/admin*",
            "allowedRoles": ["authenticated"]
        },
        {
            "route": "/login",
            "redirect": "/.auth/login/github"
        }
    ],
    "responseOverrides": {
        "401": {
            "rewrite": "/login",
            "statusCode": 302
        }
    }
}